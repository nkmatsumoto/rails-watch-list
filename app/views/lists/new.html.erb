<div class="container">
  <h1>Add a List</h1>

  <%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit 'Submit', class: 'btn btn-info mt-2' %>
  <% end %>

</div>
